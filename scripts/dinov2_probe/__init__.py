# DINOv2 Frozen Linear Probe Experiment
# Tests whether frozen DINOv2-Small-Registers features can separate 9 disease
# classes in a domain-invariant way, solving the backbone domain shift problem
# that causes Model 2 to fail on real-world field photos.
